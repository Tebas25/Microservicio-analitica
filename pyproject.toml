[project]
name = "micro-analitica"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tebas25", email = "andres.almeida.yanez@udla.edu.ec" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
micro-analitica = "micro_analitica:main"

[dependency-groups]
dev = [
    "black>=26.5.1",
    "flake8>=7.3.0",
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

